all:
  hosts:
    security-server:
      ansible_host: "{{ security_server_ip }}"
      ansible_user: deploy
      ansible_ssh_private_key_file: ~/.ssh/security_platform_key
  vars:
    platform_domain: security.omega-energy.ru
    deploy_user: deploy
    app_dir: /opt/security-platform
    docker_compose_version: "2.27.0"
